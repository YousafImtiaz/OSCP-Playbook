> On Windows:

```
.\chisel.exe client --max-retry-count 3 <kali ip>:8080 R:<forward port>:localhost:<forward port>
```

> On Linux:

```
./chisel client --max-retry-count 3 <kali ip>:8080 R:<forward port>:localhost:<forward port>
```
