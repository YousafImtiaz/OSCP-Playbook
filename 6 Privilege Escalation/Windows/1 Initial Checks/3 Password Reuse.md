> Look at what other users are on the machine and if you have a password from before, try it against those accounts to see if it works by using netexec with the list of users or try using runas:

```
runas /user:<username> cmd
```


